# 🚀 最大子数组和（LeetCode 53）

# 🧠 题目

给定整数数组 `nums` ，找到 **连续子数组** 的最大和

示例：

```
nums = [-2,1,-3,4,-1,2,1,-5,4]
```

输出：

```
6
```

解释：

```
[4,-1,2,1] = 6
```

---

# 🎯 核心思路（动态规划）

这是 **动态规划经典题（Kadane算法）**

定义：

```
dp[i] 表示以 i 结尾的最大子数组和
```

---

# 🔥 状态转移方程

核心公式：

```
dp[i] = max(nums[i], dp[i-1] + nums[i])
```

含义：

两种选择：

### ① 重新开始

```
nums[i]
```

### ② 接着前面

```
dp[i-1] + nums[i]
```

取最大即可

---

# 💻 代码

```cpp
class Solution 
{
public:
    int maxSubArray(vector<int>& nums) 
    {
        vector<int> dp(nums.size() + 1);

        dp[0] = 0;

        // dp[i] 表示以 i 位置为结尾的 最大子数组和
        for (int i = 0; i < nums.size(); ++i)
        {
            dp[i + 1] = max(nums[i], dp[i] + nums[i]);
        }

        int ret = INT_MIN;

        for (int i = 1; i < dp.size(); ++i)
        {
            if (dp[i] > ret)
                ret = dp[i];
        }

        return ret;
    }
};
```

---

# 🧩 图解

```
nums = [-2,1,-3,4,-1,2,1,-5,4]
```

计算过程：

| i | nums | dp | 最大值 |
| - | ---- | -- | --- |
| 0 | -2   | -2 | -2  |
| 1 | 1    | 1  | 1   |
| 2 | -3   | -2 | 1   |
| 3 | 4    | 4  | 4   |
| 4 | -1   | 3  | 4   |
| 5 | 2    | 5  | 5   |
| 6 | 1    | 6  | 6   |
| 7 | -5   | 1  | 6   |
| 8 | 4    | 5  | 6   |

最终：

```
最大值 = 6
```

---

# ⚠️ 易错点

## ❌ 忘记考虑负数

必须：

```
ret = INT_MIN
```

否则全负数会出错

例如：

```
[-3,-2,-1]
```

---

# ⏱️ 复杂度

时间复杂度：

```
O(n)
```

空间复杂度：

```
O(n)
```

---

# 🚀 优化写法（面试推荐）

可以优化成 **O(1) 空间**

```cpp
int maxSubArray(vector<int>& nums) 
{
    int cur = nums[0];
    int ret = nums[0];

    for (int i = 1; i < nums.size(); i++)
    {
        cur = max(nums[i], cur + nums[i]);
        ret = max(ret, cur);
    }

    return ret;
}
```

---

# 🧠 面试总结一句话

👉 **如果前面和为负，就重新开始**

---

# 💡 一句话记忆

👉 **负数断开，正数累加**

---

# 🚀 这题属于经典DP题

常见变形：

| 题目  | 类型      |
| --- | ------- |
| 53  | 最大子数组   |
| 152 | 最大乘积子数组 |
| 918 | 环形最大子数组 |

---

# 🏁 总结

这题属于：

👉 **动态规划 + 贪心思想**

腾讯 / 字节 高频 ⭐⭐⭐⭐