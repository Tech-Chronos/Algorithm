# 703. 数据流中的第 K 大元素（最小堆）

## 一、题目理解

设计一个类，支持动态加入元素，并实时返回当前数据流中的 **第 k 大元素**。

例如：
`k = 3`

当前数字：

`[4,5,8,2]`

排序后：

`[2,4,5,8]`

第 3 大 = `4`

---

## 二、核心思路（最小堆维护前 k 大）

我们不需要保存所有元素，只需要保存 **最大的 k 个数**。

使用：

```cpp
priority_queue<int, vector<int>, greater<int>>
```

这是 **小根堆（最小堆）**

堆中始终维护当前最大的 k 个元素：

* 堆顶 = 这 k 个数里最小的那个
* 也就是 **整体第 k 大元素**

---

## 三、为什么可行？

假设：

```cpp
k = 3
数据流 = [4,5,8,2]
```

维护堆：

```cpp
[4,5,8]
```

最小值是 `4`

说明：

* 比 4 大的有两个数：5、8
* 所以 4 正好是第 3 大

---

## 四、构造函数逻辑

```cpp
KthLargest(int k, vector<int>& nums)
```

初始化已有数组。

遍历 nums：

### 情况1：堆大小 < k

直接加入。

### 情况2：堆满了

若当前数 > 堆顶：

说明它能进入前 k 大。

操作：

```cpp
pop();
push();
```

否则忽略。

---

## 五、add(val) 逻辑

每次插入新数字：

### 如果堆没满：

直接加入。

### 如果堆满：

若：

```cpp
val > heap.top()
```

说明它比当前第 k 大还大，应该进入前 k 大。

替换堆顶。

最后返回：

```cpp
heap.top()
```

即第 k 大元素。

---

## 六、举例过程

```cpp
k = 3
nums = [4,5,8,2]
```

初始化后堆：

```cpp
[4,5,8]
```

---

### add(3)

3 < 4

忽略

返回：

```cpp
4
```

---

### add(10)

10 > 4

替换：

```cpp
[5,8,10]
```

返回：

```cpp
5
```

---

### add(9)

替换：

```cpp
[8,9,10]
```

返回：

```cpp
8
```

---

## 七、时间复杂度

### 构造函数：

n 个元素，每次堆操作 O(logk)

```cpp
O(n logk)
```

### add：

```cpp
O(logk)
```

---

## 八、面试官想听的点

### 为什么用最小堆，不用最大堆？

因为要找第 k 大：

维护 k 个最大元素即可。

堆顶就是第 k 大。

---

### 为什么不是排序？

每次 add 都排序：

```cpp
O(nlogn)
```

太慢。

堆只需：

```cpp
O(logk)
```

---

### 为什么 size < k 时直接加？

因为还没凑够前 k 大元素。

---

## 九、代码关键点

```cpp
priority_queue<int, vector<int>, greater<int>>
```

表示小根堆。

---

```cpp
if(val > heap.top())
```

只有更大的值才有资格进入前 k。

---

## 十、一句话总结

> 用大小为 k 的最小堆维护当前最大的 k 个数，堆顶就是第 k 大元素。

---

## 十一、这题本质

属于：

* Top K 问题
* 堆经典题
* 数据流维护题
* 高频面试题（字节/腾讯常问）

---